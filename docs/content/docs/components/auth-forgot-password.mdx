---
title: ForgotPassword
description: Password reset request form via email (reset link) or phone number (OTP), with optional tab switching.
---

## Installation

```bash tab="pnpm"
pnpm dlx shadcn@latest add REGISTRY_URL/auth-forgot-password
```

```bash tab="npm"
npx shadcn@latest add REGISTRY_URL/auth-forgot-password
```

Files added to your project:

<ComponentDepsTree name="auth-forgot-password" />

## Usage

### Email Only (Default)

<ComponentPreview name="forgot-password/email-only" />

### With Phone Support (OTP)

<ComponentPreview name="forgot-password/with-phone" />

## Components

### `<ForgotPassword>`

The root provider component that manages the password recovery state, forms, and context.

<AutoTypeTable path="../src/components/auth/forgot-password.tsx" name="ForgotPasswordProps" />

### `<ForgotPassword.Header>`

Displays the title and description at the top of the card. The description dynamically updates depending on whether the email or phone tab is active.

<AutoTypeTable path="../src/components/auth/forgot-password.tsx" name="ForgotPasswordHeaderProps" />

### `<ForgotPassword.Content>`

The main container for the forgot password forms. It automatically displays the `apiError` banner if an error is present.

<AutoTypeTable path="../src/components/auth/forgot-password.tsx" name="ForgotPasswordContentProps" />

### `<ForgotPassword.Tabs>`

Provides a tabbed interface to switch between "Email" and "Phone" recovery methods. If `allowPhone={false}` on the root provider, this component renders its children directly without tabs.

<AutoTypeTable path="../src/components/auth/forgot-password.tsx" name="ForgotPasswordTabsProps" />

### `<ForgotPassword.EmailForm>`

A wrapper around the form elements specifically for the email recovery flow. Automatically hides itself if the phone tab is active.

<AutoTypeTable path="../src/components/auth/forgot-password.tsx" name="ForgotPasswordEmailFormProps" />

### `<ForgotPassword.PhoneForm>`

A wrapper around the form elements specifically for the phone recovery flow. Automatically hides itself if the email tab is active.

<AutoTypeTable path="../src/components/auth/forgot-password.tsx" name="ForgotPasswordPhoneFormProps" />

### `<ForgotPassword.EmailField>`

Renders an input field for the user to enter their email address.

### `<ForgotPassword.PhoneField>`

Renders a specialized phone input field with country code selection for the user to enter their phone number.

### `<ForgotPassword.SubmitButton>`

The primary button to submit the recovery form. Automatically adapts its label ("Send Reset Link" vs "Send OTP") based on the active tab and shows a loading state.

<AutoTypeTable path="../src/components/auth/forgot-password.tsx" name="ForgotPasswordSubmitButtonProps" />

### `<ForgotPasswordSuccessView>`

A standalone success screen component that you can render after a successful reset request.

<AutoTypeTable path="../src/components/auth/forgot-password.tsx" name="ForgotPasswordSuccessViewProps" />

## Types

```ts
type ForgotPasswordEmailValues = {
  email: string;
};

type ForgotPasswordPhoneValues = {
  phone: string; // E.164 format
};
```

## Behaviour

- When `allowPhone={false}` (default), `ForgotPassword.Tabs` renders children directly with no tab UI.
- When `allowPhone={true}`, a `<Tabs>` with "Email" and "Phone" triggers is shown. The inactive form returns `null` automatically.
- The submit button adapts its label based on the `activeTab`.
- `ForgotPasswordSuccessView` is standalone — manage the `sent` state outside and swap to it yourself.

